---
title: "System::Array::LastIndexOf metode"
linktitle: "LastIndexOf"
second_title: "Aspose.Page untuk C++"
description: "System::Array::LastIndexOf method. Menentukan indeks kemunculan terakhir dari item yang ditentukan dalam rentang item array yang ditentukan oleh indeks mulai dan jumlah elemen dalam rentang tersebut dalam C++."
type: docs
weight: 5500
url: /id/cpp/system/array/lastindexof/
---
## Array::LastIndexOf(const ArrayPtr\<ArrayType\>\&, const ValueType\&, int, int) method


Menentukan indeks kemunculan terakhir dari item yang ditentukan dalam rentang item array yang ditentukan oleh indeks mulai dan jumlah elemen dalam rentang tersebut.

```cpp
template<typename ArrayType,typename ValueType> static int System::Array<T>::LastIndexOf(const ArrayPtr<ArrayType> &arr, const ValueType &value, int startIndex, int count)
```


| Parameter | Deskripsi |
| --- | --- |
| ArrayType | Tipe elemen dalam array target |
| ValueType | tipe item yang akan dicari dalam array |

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| arr | const ArrayPtr\<ArrayType\>\& | [Array](../) untuk mencari item yang ditentukan di dalamnya |
| value | const ValueType\& | Indeks item yang akan ditentukan |
| startIndex | int | Indeks di mana pencarian dimulai |
| count | int | Jumlah elemen dalam rentang yang akan dicari |

### ReturnValue

Indeks dari kemunculan terakhir dari item yang ditentukan jika item ditemukan, jika tidak -1

## Lihat Juga

* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [ValueType](../valuetype/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Array::LastIndexOf(const ArrayPtr\<ArrayType\>\&, const ValueType\&) method


Menentukan indeks kemunculan terakhir dari item yang ditentukan dalam array.

```cpp
template<typename ArrayType,typename ValueType> static int System::Array<T>::LastIndexOf(const ArrayPtr<ArrayType> &items, const ValueType &value)
```


| Parameter | Deskripsi |
| --- | --- |
| ArrayType | Tipe elemen dalam array target |
| ValueType | tipe item yang akan dicari dalam array |

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| items | const ArrayPtr\<ArrayType\>\& | [Array](../) untuk mencari item yang ditentukan di dalamnya |
| value | const ValueType\& | Indeks item yang akan ditentukan |

### ReturnValue

Indeks dari kemunculan terakhir dari item yang ditentukan jika item ditemukan, jika tidak -1

## Lihat Juga

* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [ValueType](../valuetype/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Array::LastIndexOf(const ArrayPtr\<ArrayType\>\&, const ValueType\&, int) method


Menentukan indeks kemunculan terakhir dari item yang ditentukan dalam array mulai dari indeks yang ditentukan.

```cpp
template<typename ArrayType,typename ValueType> static int System::Array<T>::LastIndexOf(const ArrayPtr<ArrayType> &items, const ValueType &value, int startIndex)
```


| Parameter | Deskripsi |
| --- | --- |
| ArrayType | Tipe elemen dalam array target |
| ValueType | tipe item yang akan dicari dalam array |

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| items | const ArrayPtr\<ArrayType\>\& | [Array](../) untuk mencari item yang ditentukan di dalamnya |
| value | const ValueType\& | Indeks item yang akan ditentukan |
| startIndex | int | Indeks di mana pencarian dimulai |

### ReturnValue

Indeks dari kemunculan terakhir dari item yang ditentukan jika item ditemukan, jika tidak -1

## Lihat Juga

* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [ValueType](../valuetype/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
