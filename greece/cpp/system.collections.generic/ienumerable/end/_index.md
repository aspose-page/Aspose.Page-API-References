---
title: "System::Collections::Generic::IEnumerable::end μέθοδος"
linktitle: "end"
second_title: "Aspose.Page για C++"
description: "System::Collections::Generic::IEnumerable::end μέθοδος. Επιστρέφει iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) της συλλογής. Αυτός ο iterator δεν μπορεί να χρησιμοποιηθεί για την αλλαγή ενός αναφερόμενου αντικειμένου επειδή η GetEnumerator() επιστρέφει ένα αντικείμενο‑αντίγραφο του T σε C++."
type: docs
weight: 400
url: /el/cpp/system.collections.generic/ienumerable/end/
---
## IEnumerable::end() method


Επιστρέφει iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) της συλλογής. Αυτός ο iterator δεν μπορεί να χρησιμοποιηθεί για την αλλαγή ενός αναφερόμενου αντικειμένου επειδή το [GetEnumerator()](../getenumerator/) επιστρέφει ένα αντικείμενο‑αντίγραφο του T.

```cpp
iterator System::Collections::Generic::IEnumerable<T>::end()
```


### ReturnValue

Ένας επαναλήπτης που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εάν υπάρχει) της συλλογής

## Δείτε επίσης

* Typedef [iterator](../iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Page for C++](../../../)
## IEnumerable::end() const method


Λαμβάνει επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της const-qualified παρουσίασης της συλλογής.

```cpp
const_iterator System::Collections::Generic::IEnumerable<T>::end() const
```


### ReturnValue

Ένας επαναλήπτης που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εάν υπάρχει) της const‑προσδιορισμένης παρουσίασης της συλλογής

## Δείτε επίσης

* Typedef [const_iterator](../const_iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Page for C++](../../../)
