---
title: "Méthode System::Char::IsLower"
linktitle: "IsLower"
second_title: "Aspose.Page pour C++"
description: "Méthode System::Char::IsLower. Détermine si le caractère spécifié est classé comme une lettre minuscule en C++."
type: docs
weight: 1100
url: /fr/cpp/system/char/islower/
---
## Char::IsLower(char_t) method


Détermine si le caractère spécifié est classé comme une lettre minuscule.

```cpp
static bool System::Char::IsLower(char_t c)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### ReturnValue

Vrai si le caractère spécifié est une lettre minuscule, sinon - false

## Voir aussi

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Char::IsLower(const char_t *, int) method


Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme une lettre minuscule.

```cpp
static bool System::Char::IsLower(const char_t *str, int idx)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Un indice zéro‑basé dans le tampon spécifié du caractère à tester |

### ReturnValue

Vrai si le caractère à l'index spécifié est une lettre minuscule, sinon - false

## Voir aussi

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Char::IsLower(const String\&, int) method


Détermine si le caractère à l'index spécifié dans la chaîne spécifiée est classé comme une lettre minuscule.

```cpp
static bool System::Char::IsLower(const String &str, int idx)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| str | const String\& | Une chaîne |
| idx | int | Un indice zéro‑basé dans la chaîne spécifiée à tester |

### ReturnValue

Vrai si le caractère à l'index spécifié est une lettre minuscule, sinon - false

## Voir aussi

* Class [String](../../string/)
* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
