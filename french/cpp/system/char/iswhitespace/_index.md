---
title: "Méthode System::Char::IsWhiteSpace"
linktitle: "IsWhiteSpace"
second_title: "Aspose.Page pour C++"
description: "Méthode System::Char::IsWhiteSpace. Détermine si le caractère spécifié est classé comme un caractère d'espace blanc en C++."
type: docs
weight: 2000
url: /fr/cpp/system/char/iswhitespace/
---
## Char::IsWhiteSpace(char_t) method


Détermine si le caractère spécifié est classé comme un caractère d'espace blanc.

```cpp
static bool System::Char::IsWhiteSpace(char_t c)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### ReturnValue

Vrai si le caractère spécifié est un caractère d'espace blanc, sinon - false

## Voir aussi

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Char::IsWhiteSpace(const char_t *, int) method


Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme un caractère d'espace blanc.

```cpp
static bool System::Char::IsWhiteSpace(const char_t *str, int idx)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Un indice zéro‑basé dans le tampon spécifié du caractère à tester |

### ReturnValue

Vrai si le caractère à l'index spécifié est un caractère d'espace blanc, sinon - false

## Voir aussi

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Char::IsWhiteSpace(const String\&, int) method


Détermine si le caractère à l'index spécifié dans la chaîne spécifiée est classé comme un caractère d'espace blanc.

```cpp
static bool System::Char::IsWhiteSpace(const String &str, int index)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| str | const String\& | Une chaîne |
| indice | int | Un indice basé sur zéro dans la chaîne spécifiée |

### ReturnValue

Vrai si le caractère à l'index spécifié est un caractère d'espace blanc, sinon - false

## Voir aussi

* Class [String](../../string/)
* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
