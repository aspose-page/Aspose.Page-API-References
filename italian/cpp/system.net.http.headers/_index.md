---
title: "Namespace System::Net::Http::Headers"
linktitle: "System::Net::Http::Headers"
second_title: "Aspose.Page per C++"
description: "Come utilizzare il namespace System::Net::Http::Headers in C++."
type: docs
weight: 4500
url: /it/cpp/system.net.http.headers/
---



## Classi

| Classe | Descrizione |
| --- | --- |
| [AuthenticationHeaderValue](./authenticationheadervalue/) | Rappresenta i valori delle intestazioni 'Authorization', 'ProxyAuthorization', 'WWW-Authenticate' e 'Proxy-Authenticate'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [CacheControlHeaderValue](./cachecontrolheadervalue/) | Rappresenta un valore dell'intestazione 'Cache-Control'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [ContentDispositionHeaderValue](./contentdispositionheadervalue/) | Rappresenta un valore dell'intestazione 'Content-Disposition'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [ContentRangeHeaderValue](./contentrangeheadervalue/) | Rappresenta un valore dell'intestazione 'Content-Range'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [EntityTagHeaderValue](./entitytagheadervalue/) | Rappresenta un valore dell'intestazione 'Entity-Tag'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [HttpContentHeaders](./httpcontentheaders/) | Rappresenta la raccolta delle intestazioni 'Content'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [HttpHeaders](./httpheaders/) | La raccolta delle intestazioni HTTP. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [HttpHeaderValueCollection](./httpheadervaluecollection/) | Rappresenta la raccolta dei valori delle intestazioni. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [String >](./string_/) | La specializzazione parziale del template [HttpHeaderValueCollection](./httpheadervaluecollection/) per il tipo [String](../system/string/). Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [HttpRequestHeaders](./httprequestheaders/) | Rappresenta la raccolta delle intestazioni 'Request'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [HttpResponseHeaders](./httpresponseheaders/) | Rappresenta la raccolta delle intestazioni 'Response'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [MediaTypeHeaderValue](./mediatypeheadervalue/) | Rappresenta un tipo MIME in un valore dell'intestazione 'Content-Type'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [MediaTypeWithQualityHeaderValue](./mediatypewithqualityheadervalue/) | Rappresenta un tipo MIME con un fattore di qualità aggiuntivo in un valore dell'intestazione 'Content-Type'. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [NameValueHeaderValue](./namevalueheadervalue/) | Rappresenta una coppia chiave/valore da utilizzare nelle intestazioni. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [NameValueWithParametersHeaderValue](./namevaluewithparametersheadervalue/) | Rappresenta una coppia chiave/valore con parametri da utilizzare nelle intestazioni. Gli oggetti di questa classe devono essere allocati solo utilizzando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e utilizza questo puntatore per passarlo alle funzioni come argomento. |
| [ObjectCollection](./objectcollection/) | Rappresenta la raccolta degli oggetti. |
| [ProductHeaderValue](./productheadervalue/) | Rappresenta un token di prodotto in un valore dell'intestazione 'User-Agent'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [ProductInfoHeaderValue](./productinfoheadervalue/) | Rappresenta un prodotto o un commento in un valore dell'intestazione 'User-Agent'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò proverà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [RangeConditionHeaderValue](./rangeconditionheadervalue/) | Rappresenta un valore dell'intestazione 'If-Range'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [RangeHeaderValue](./rangeheadervalue/) | Rappresenta un valore dell'intestazione 'Range'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [RangeItemHeaderValue](./rangeitemheadervalue/) | Rappresenta un intervallo di byte in un valore dell'intestazione 'Range'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [RetryConditionHeaderValue](./retryconditionheadervalue/) | Rappresenta un valore dell'intestazione 'Retry-After'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [StringWithQualityHeaderValue](./stringwithqualityheadervalue/) | Rappresenta un valore con una qualità aggiuntiva di un'intestazione stringa. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [TransferCodingHeaderValue](./transfercodingheadervalue/) | Rappresenta un valore dell'intestazione 'Accept-Encoding'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [TransferCodingWithQualityHeaderValue](./transfercodingwithqualityheadervalue/) | Rappresenta un valore con una qualità aggiuntiva dell'intestazione 'Accept-Encoding'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [ViaHeaderValue](./viaheadervalue/) | Rappresenta un valore dell'intestazione 'Via'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
| [WarningHeaderValue](./warningheadervalue/) | Rappresenta un valore dell'intestazione 'Warning'. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/) . Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarla alle funzioni come argomento. |
## Typedefs

| Typedef | Descrizione |
| --- | --- |
| [HeaderFunc](./headerfunc/) |  |
