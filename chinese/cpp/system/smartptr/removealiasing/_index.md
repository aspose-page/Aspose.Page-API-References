---
title: "System::SmartPtr::RemoveAliasing 方法"
linktitle: "RemoveAliasing"
second_title: "Aspose.Page 适用于 C++"
description: "System::SmartPtr::RemoveAliasing 方法。移除指针的别名（由别名构造函数创建），确保它管理（如果是共享的）或跟踪（如果是弱引用的）它所指向的同一对象，在 C++ 中。"
type: docs
weight: 3100
url: /zh/cpp/system/smartptr/removealiasing/
---
## SmartPtr::RemoveAliasing method


从指针中移除别名（由别名构造函数创建），确保它管理（如果是共享）或跟踪（如果是弱引用）指向的相同对象。

```cpp
SmartPtr_ System::SmartPtr<T>::RemoveAliasing() const
```


### ReturnValue

指向同一对象的指针，该指针负责该对象的生命周期管理。

## 另见

* Typedef [SmartPtr_](../smartptr_/)
* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
