---
title: "System::SmartPtr::begin 方法"
linktitle: "begin"
second_title: "Aspose.Page 适用于 C++"
description: "System::SmartPtr::begin 方法。底层集合的 begin() 方法的访问器。仅当 SmartPtr_ 在 C++ 中是具有 begin() 方法的特化类型时才会编译。"
type: docs
weight: 300
url: /zh/cpp/system/smartptr/begin/
---
## SmartPtr::begin() method


对底层集合的 [begin()](./) 方法的访问器。仅当 [SmartPtr_](../smartptr_/) 是具有 [begin()](./) 方法的特化类型时才会编译。

```cpp
template<typename Q> decltype(std::declval<Q>().begin()) System::SmartPtr<T>::begin()
```


### ReturnValue

指向集合起始位置的迭代器

## 另见

* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## SmartPtr::begin() const method


对底层集合的 [begin()](./) 方法的访问器。仅当 [SmartPtr_](../smartptr_/) 是具有 [begin()](./) 方法的特化类型时才会编译。

```cpp
template<typename Q> decltype(std::declval<const Q>().begin()) System::SmartPtr<T>::begin() const
```


### ReturnValue

指向集合起始位置的迭代器

## 另见

* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
