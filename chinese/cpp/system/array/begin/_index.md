---
title: "System::Array::begin 方法"
linktitle: "begin"
second_title: "Aspose.Page 适用于 C++"
description: "System::Array::begin 方法。返回指向 const 限定容器的第一个元素的迭代器。如果容器为空，返回的迭代器将等于 C++ 中的 end()。"
type: docs
weight: 300
url: /zh/cpp/system/array/begin/
---
## Array::begin() const method


返回一个迭代器，指向 const 限定容器的第一个元素。如果容器为空，返回的迭代器将等于 [end()](../end/)。

```cpp
const_iterator System::Array<T>::begin() const noexcept
```


### ReturnValue

一个迭代器，指向 const 限定容器的第一个元素。

## 另见

* Typedef [const_iterator](../const_iterator/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Array::begin() method


返回一个迭代器，指向容器的第一个元素。如果容器为空，返回的迭代器将等于 [end()](../end/)。

```cpp
iterator System::Array<T>::begin() noexcept
```


### ReturnValue

一个迭代器，指向容器的第一个元素。

## 另见

* Typedef [iterator](../iterator/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
