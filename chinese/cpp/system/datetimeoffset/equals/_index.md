---
title: "System::DateTimeOffset::Equals 方法"
linktitle: "等于"
second_title: "Aspose.Page 适用于 C++"
description: "System::DateTimeOffset::Equals 方法。检查两个 DateTimeOffset 对象在 C++ 中是否表示相同的时间点。"
type: docs
weight: 1200
url: /zh/cpp/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const method


检查两个 [DateTimeOffset](../) 对象是否表示相同的时间点。

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


| Parameter | Type | 描述 |
| --- | --- | --- |
| other | const DateTimeOffset\& | 用于比较的 [Object](../../object/)。 |

### ReturnValue

如果当前对象等于 **other**，则为 true，否则为 false。

## 另见

* Class [DateTimeOffset](../)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const method


检查两个 [DateTimeOffset](../) 对象是否表示相同的时间点。

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


| Parameter | Type | 描述 |
| --- | --- | --- |
| obj | const SharedPtr\<Object\>\& | 用于比较的 [Object](../../object/)。 |

### ReturnValue

如果当前对象等于 **obj**，则为 true，否则为 false。

## 另见

* Typedef [SharedPtr](../../sharedptr/)
* Class [Object](../../object/)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) method


检查两个 [DateTimeOffset](../) 对象是否表示相同的时间点。

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


| Parameter | Type | 描述 |
| --- | --- | --- |
| 第一个 | const DateTimeOffset\& | 要比较的第一个对象。 |
| 秒 | const DateTimeOffset\& | 要比较的第二个对象。 |

### ReturnValue

如果 **first** 等于 **second**，则为 true，否则为 false。

## 另见

* Class [DateTimeOffset](../)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
