---
title: "System::Char::IsLower 方法"
linktitle: "IsLower"
second_title: "Aspose.Page 适用于 C++"
description: "System::Char::IsLower 方法。确定指定字符在 C++ 中是否被归类为小写字母。"
type: docs
weight: 1100
url: /zh/cpp/system/char/islower/
---
## Char::IsLower(char_t) method


确定指定字符是否被归类为小写字母。

```cpp
static bool System::Char::IsLower(char_t c)
```


| Parameter | Type | 描述 |
| --- | --- | --- |
| c | char_t | 要测试的字符 |

### ReturnValue

如果指定字符是小写字母，则为 true；否则为 false

## 另见

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Char::IsLower(const char_t *, int) method


确定在指定字符缓冲区中指定索引处的字符是否被归类为小写字母。

```cpp
static bool System::Char::IsLower(const char_t *str, int idx)
```


| Parameter | Type | 描述 |
| --- | --- | --- |
| str | const char_t * | 指向字符缓冲区起始位置的指针 |
| idx | int | 在指定缓冲区中要测试的字符的零基索引 |

### ReturnValue

如果指定索引处的字符是小写字母，则为 true；否则为 false

## 另见

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
## Char::IsLower(const String\&, int) method


确定在指定字符串中指定索引处的字符是否被归类为小写字母。

```cpp
static bool System::Char::IsLower(const String &str, int idx)
```


| Parameter | Type | 描述 |
| --- | --- | --- |
| str | const String\& | 一个字符串 |
| idx | int | 在指定字符串中要测试的字符的零基索引 |

### ReturnValue

如果指定索引处的字符是小写字母，则为 true；否则为 false

## 另见

* Class [String](../../string/)
* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
