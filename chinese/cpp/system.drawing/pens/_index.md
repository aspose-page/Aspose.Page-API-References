---
title: "System::Drawing::Pens 类"
linktitle: "笔"
second_title: "Aspose.Page 适用于 C++"
description: "System::Drawing::Pens 类。提供一组预先创建的 Pen 对象。这是一个没有实例服务的静态类型。您绝不应该以任何方式在 C++ 中创建其实例。"
type: docs
weight: 1600
url: /zh/cpp/system.drawing/pens/
---
## Pens class


提供一组预先创建的 [Pen](../pen/) 对象。这是一个没有实例服务的静态类型。您绝不应该以任何方式创建其实例。

```cpp
class Pens
```

## 方法

| 方法 | 描述 |
| --- | --- |
| static [get_AliceBlue](./get_aliceblue/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFF0F8FF 的 [Pen](../pen/) 对象。 |
| static [get_AntiqueWhite](./get_antiquewhite/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFFAEBD7 的 [Pen](../pen/) 对象。 |
| static [get_Aqua](./get_aqua/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF00FFFF 的 [Pen](../pen/) 对象。 |
| static [get_Aquamarine](./get_aquamarine/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF7FFFD4 的 [Pen](../pen/) 对象。 |
| static [get_Azure](./get_azure/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFF0FFFF 的 [Pen](../pen/) 对象。 |
| static [get_Beige](./get_beige/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFF5F5DC 的 [Pen](../pen/) 对象。 |
| static [get_Bisque](./get_bisque/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFFFE4C4 的 [Pen](../pen/) 对象。 |
| static [get_Black](./get_black/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF000000 的 [Pen](../pen/) 对象。 |
| static [get_BlanchedAlmond](./get_blanchedalmond/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFFFEBCD 的 [Pen](../pen/) 对象。 |
| static [get_Blue](./get_blue/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF0000FF 的 [Pen](../pen/) 对象。 |
| static [get_BlueViolet](./get_blueviolet/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF8A2BE2 的 [Pen](../pen/) 对象。 |
| static [get_Brown](./get_brown/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFA52A2A 的 [Pen](../pen/) 对象。 |
| static [get_BurlyWood](./get_burlywood/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFDEB887 的 [Pen](../pen/) 对象。 |
| static [get_CadetBlue](./get_cadetblue/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF5F9EA0 的 [Pen](../pen/) 对象。 |
| static [get_Chartreuse](./get_chartreuse/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF7FFF00 的 [Pen](../pen/) 对象。 |
| static [get_Chocolate](./get_chocolate/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFD2691E 的 [Pen](../pen/) 对象。 |
| static [get_Coral](./get_coral/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FFFF7F50 的 [Pen](../pen/) 对象。 |
| static [get_CornflowerBlue](./get_cornflowerblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF6495ED。 |
| static [get_Cornsilk](./get_cornsilk/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFFF8DC。 |
| static [get_Crimson](./get_crimson/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFDC143C。 |
| static [get_Cyan](./get_cyan/)() | 返回一个宽度为 1、ARGB 颜色十六进制表示为 #FF00FFFF 的 [Pen](../pen/) 对象。 |
| static [get_DarkBlue](./get_darkblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF00008B。 |
| static [get_DarkCyan](./get_darkcyan/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF008B8B。 |
| static [get_DarkGoldenrod](./get_darkgoldenrod/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFB8860B。 |
| static [get_DarkGray](./get_darkgray/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFA9A9A9。 |
| static [get_DarkGreen](./get_darkgreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF006400。 |
| static [get_DarkKhaki](./get_darkkhaki/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFBDB76B。 |
| static [get_DarkMagenta](./get_darkmagenta/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF8B008B。 |
| static [get_DarkOliveGreen](./get_darkolivegreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF556B2F。 |
| static [get_DarkOrange](./get_darkorange/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFF8C00。 |
| static [get_DarkOrchid](./get_darkorchid/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF9932CC。 |
| static [get_DarkRed](./get_darkred/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF8B0000。 |
| static [get_DarkSalmon](./get_darksalmon/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFE9967A。 |
| static [get_DarkSeaGreen](./get_darkseagreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF8FBC8F。 |
| static [get_DarkSlateBlue](./get_darkslateblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF483D8B。 |
| static [get_DarkSlateGray](./get_darkslategray/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF2F4F4F。 |
| static [get_DarkTurquoise](./get_darkturquoise/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF00CED1。 |
| static [get_DarkViolet](./get_darkviolet/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF9400D3。 |
| static [get_DeepPink](./get_deeppink/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFF1493。 |
| static [get_DeepSkyBlue](./get_deepskyblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF00BFFF。 |
| static [get_DimGray](./get_dimgray/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF696969。 |
| static [get_DodgerBlue](./get_dodgerblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF1E90FF。 |
| static [get_Firebrick](./get_firebrick/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFB22222。 |
| static [get_FloralWhite](./get_floralwhite/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFFAF0 的 [Pen](../pen/) 对象。 |
| static [get_ForestGreen](./get_forestgreen/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF228B22 的 [Pen](../pen/) 对象。 |
| static [get_Fuchsia](./get_fuchsia/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFF00FF 的 [Pen](../pen/) 对象。 |
| static [get_Gainsboro](./get_gainsboro/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFDCDCDC 的 [Pen](../pen/) 对象。 |
| static [get_GhostWhite](./get_ghostwhite/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFF8F8FF 的 [Pen](../pen/) 对象。 |
| static [get_Gold](./get_gold/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFD700 的 [Pen](../pen/) 对象。 |
| static [get_Goldenrod](./get_goldenrod/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFDAA520 的 [Pen](../pen/) 对象。 |
| static [get_Gray](./get_gray/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF808080 的 [Pen](../pen/) 对象。 |
| static [get_Green](./get_green/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF008000 的 [Pen](../pen/) 对象。 |
| static [get_GreenYellow](./get_greenyellow/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFADFF2F 的 [Pen](../pen/) 对象。 |
| static [get_Honeydew](./get_honeydew/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFF0FFF0 的 [Pen](../pen/) 对象。 |
| static [get_HotPink](./get_hotpink/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFF69B4 的 [Pen](../pen/) 对象。 |
| static [get_IndianRed](./get_indianred/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFCD5C5C 的 [Pen](../pen/) 对象。 |
| static [get_Indigo](./get_indigo/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF4B0082 的 [Pen](../pen/) 对象。 |
| static [get_Ivory](./get_ivory/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFFFF0 的 [Pen](../pen/) 对象。 |
| static [get_Khaki](./get_khaki/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFF0E68C 的 [Pen](../pen/) 对象。 |
| static [get_Lavender](./get_lavender/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFE6E6FA 的 [Pen](../pen/) 对象。 |
| static [get_LavenderBlush](./get_lavenderblush/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFF0F5 的 [Pen](../pen/) 对象。 |
| static [get_LawnGreen](./get_lawngreen/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF7CFC00 的 [Pen](../pen/) 对象。 |
| static [get_LemonChiffon](./get_lemonchiffon/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFFACD 的 [Pen](../pen/) 对象。 |
| static [get_LightBlue](./get_lightblue/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFADD8E6 的 [Pen](../pen/) 对象。 |
| static [get_LightCoral](./get_lightcoral/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFF08080 的 [Pen](../pen/) 对象。 |
| static [get_LightCyan](./get_lightcyan/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFE0FFFF 的 [Pen](../pen/) 对象。 |
| static [get_LightGoldenrodYellow](./get_lightgoldenrodyellow/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFAFAD2 的 [Pen](../pen/) 对象。 |
| static [get_LightGray](./get_lightgray/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFD3D3D3 的 [Pen](../pen/) 对象。 |
| static [get_LightGreen](./get_lightgreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF90EE90。 |
| static [get_LightPink](./get_lightpink/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFFB6C1。 |
| static [get_LightSalmon](./get_lightsalmon/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFFA07A。 |
| static [get_LightSeaGreen](./get_lightseagreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF20B2AA。 |
| static [get_LightSkyBlue](./get_lightskyblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF87CEFA。 |
| static [get_LightSlateGray](./get_lightslategray/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF778899。 |
| static [get_LightSteelBlue](./get_lightsteelblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFB0C4DE。 |
| static [get_LightYellow](./get_lightyellow/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFFFFE0。 |
| static [get_Lime](./get_lime/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF00FF00。 |
| static [get_LimeGreen](./get_limegreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF32CD32。 |
| static [get_Linen](./get_linen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFAF0E6。 |
| static [get_Magenta](./get_magenta/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFF00FF 的 [Pen](../pen/) 对象。 |
| static [get_Maroon](./get_maroon/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF800000。 |
| static [get_MediumAquamarine](./get_mediumaquamarine/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF66CDAA。 |
| static [get_MediumBlue](./get_mediumblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF0000CD。 |
| static [get_MediumOrchid](./get_mediumorchid/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFBA55D3。 |
| static [get_MediumPurple](./get_mediumpurple/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF9370DB。 |
| static [get_MediumSeaGreen](./get_mediumseagreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF3CB371。 |
| static [get_MediumSlateBlue](./get_mediumslateblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF7B68EE。 |
| static [get_MediumSpringGreen](./get_mediumspringgreen/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF00FA9A。 |
| static [get_MediumTurquoise](./get_mediumturquoise/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF48D1CC。 |
| static [get_MediumVioletRed](./get_mediumvioletred/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFC71585。 |
| static [get_MidnightBlue](./get_midnightblue/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FF191970。 |
| static [get_MintCream](./get_mintcream/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFF5FFFA。 |
| static [get_MistyRose](./get_mistyrose/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFFE4E1。 |
| static [get_Moccasin](./get_moccasin/)() | 返回一个 [Pen](../pen/) 对象，宽度为 1，其十六进制表示的 ARGB 颜色值为 #FFFFE4B5。 |
| static [get_NavajoWhite](./get_navajowhite/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFDEAD 的 [Pen](../pen/) 对象。 |
| static [get_Navy](./get_navy/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF000080 的 [Pen](../pen/) 对象。 |
| static [get_OldLace](./get_oldlace/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFDF5E6 的 [Pen](../pen/) 对象。 |
| static [get_Olive](./get_olive/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF808000 的 [Pen](../pen/) 对象。 |
| static [get_OliveDrab](./get_olivedrab/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF6B8E23 的 [Pen](../pen/) 对象。 |
| static [get_Orange](./get_orange/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFA500 的 [Pen](../pen/) 对象。 |
| static [get_OrangeRed](./get_orangered/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFF4500 的 [Pen](../pen/) 对象。 |
| static [get_Orchid](./get_orchid/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFDA70D6 的 [Pen](../pen/) 对象。 |
| static [get_PaleGoldenrod](./get_palegoldenrod/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFEEE8AA 的 [Pen](../pen/) 对象。 |
| static [get_PaleGreen](./get_palegreen/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF98FB98 的 [Pen](../pen/) 对象。 |
| static [get_PaleTurquoise](./get_paleturquoise/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFAFEEEE 的 [Pen](../pen/) 对象。 |
| static [get_PaleVioletRed](./get_palevioletred/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFDB7093 的 [Pen](../pen/) 对象。 |
| static [get_PapayaWhip](./get_papayawhip/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFEFD5 的 [Pen](../pen/) 对象。 |
| static [get_PeachPuff](./get_peachpuff/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFDAB9 的 [Pen](../pen/) 对象。 |
| static [get_Peru](./get_peru/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFCD853F 的 [Pen](../pen/) 对象。 |
| static [get_Pink](./get_pink/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFFC0CB 的 [Pen](../pen/) 对象。 |
| static [get_Plum](./get_plum/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFDDA0DD 的 [Pen](../pen/) 对象。 |
| static [get_PowderBlue](./get_powderblue/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFB0E0E6 的 [Pen](../pen/) 对象。 |
| static [get_Purple](./get_purple/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF800080 的 [Pen](../pen/) 对象。 |
| static [get_Red](./get_red/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFF0000 的 [Pen](../pen/) 对象。 |
| static [get_RosyBrown](./get_rosybrown/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFBC8F8F 的 [Pen](../pen/) 对象。 |
| static [get_RoyalBlue](./get_royalblue/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF4169E1 的 [Pen](../pen/) 对象。 |
| static [get_SaddleBrown](./get_saddlebrown/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FF8B4513 的 [Pen](../pen/) 对象。 |
| static [get_Salmon](./get_salmon/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFFA8072 的 [Pen](../pen/) 对象。 |
| static [get_SandyBrown](./get_sandybrown/)() | 返回一个宽度为 1 且其十六进制表示的 ARGB 颜色值为 #FFF4A460 的 [Pen](../pen/) 对象。 |
| static [get_SeaGreen](./get_seagreen/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF2E8B57 的 [Pen](../pen/) 对象。 |
| static [get_SeaShell](./get_seashell/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFFFF5EE 的 [Pen](../pen/) 对象。 |
| static [get_Sienna](./get_sienna/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFA0522D 的 [Pen](../pen/) 对象。 |
| static [get_Silver](./get_silver/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFC0C0C0 的 [Pen](../pen/) 对象。 |
| static [get_SkyBlue](./get_skyblue/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF87CEEB 的 [Pen](../pen/) 对象。 |
| static [get_SlateBlue](./get_slateblue/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF6A5ACD 的 [Pen](../pen/) 对象。 |
| static [get_SlateGray](./get_slategray/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF708090 的 [Pen](../pen/) 对象。 |
| static [get_Snow](./get_snow/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFFFFAFA 的 [Pen](../pen/) 对象。 |
| static [get_SpringGreen](./get_springgreen/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF00FF7F 的 [Pen](../pen/) 对象。 |
| static [get_SteelBlue](./get_steelblue/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF4682B4 的 [Pen](../pen/) 对象。 |
| static [get_Tan](./get_tan/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFD2B48C 的 [Pen](../pen/) 对象。 |
| static [get_Teal](./get_teal/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF008080 的 [Pen](../pen/) 对象。 |
| static [get_Thistle](./get_thistle/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFD8BFD8 的 [Pen](../pen/) 对象。 |
| static [get_Tomato](./get_tomato/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFFF6347 的 [Pen](../pen/) 对象。 |
| static [get_Transparent](./get_transparent/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #00FFFFFF 的 [Pen](../pen/) 对象。 |
| static [get_Turquoise](./get_turquoise/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF40E0D0 的 [Pen](../pen/) 对象。 |
| static [get_Violet](./get_violet/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFEE82EE 的 [Pen](../pen/) 对象。 |
| static [get_Wheat](./get_wheat/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFF5DEB3 的 [Pen](../pen/) 对象。 |
| static [get_White](./get_white/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFFFFFFF 的 [Pen](../pen/) 对象。 |
| static [get_WhiteSmoke](./get_whitesmoke/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFF5F5F5 的 [Pen](../pen/) 对象。 |
| static [get_Yellow](./get_yellow/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FFFFFF00 的 [Pen](../pen/) 对象。 |
| static [get_YellowGreen](./get_yellowgreen/)() | 返回一个宽度为 1 且其 ARGB 颜色的十六进制表示值为 #FF9ACD32 的 [Pen](../pen/) 对象。 |
## 另见

* Namespace [System::Drawing](../)
* Library [Aspose.Page for C++](../../)
