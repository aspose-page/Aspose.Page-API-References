---
title: "System::Web::HttpContext 类"
linktitle: "HttpContext"
second_title: "Aspose.Page 适用于 C++"
description: "System::Web::HttpContext 类。虚拟类，使得可以将翻译后的代码与 HttpContext 引用链接，但不执行。未实现任何成员。此类的对象只能使用 System::MakeObject() 函数分配。切勿在栈上或使用 operator new 创建此类型的实例，因为会导致运行时错误和/或断言故障。始终将此类包装在 System::SmartPtr 指针中，并使用该指针将其作为参数传递给 C++ 函数。"
type: docs
weight: 100
url: /zh/cpp/system.web/httpcontext/
---
## HttpContext class


虚拟类，使得可以将翻译后的代码与 [HttpContext](./) 引用链接，但不执行。未实现任何成员。此类的对象只能使用 [System::MakeObject()](../../system/makeobject/) 函数分配。切勿在栈上或使用 operator new 创建此类型的实例，因为会导致运行时错误和/或断言故障。始终将此类包装在 [System::SmartPtr](../../system/smartptr/) 指针中，并使用该指针将其作为参数传递给函数。

```cpp
class HttpContext : public System::Object
```

## 方法

| 方法 | 描述 |
| --- | --- |
| static [get_Current](./get_current/)() | 未实现。 |
| [get_Request](./get_request/)() | 未实现。 |
| static [set_Current](./set_current/)(const System::SharedPtr\<HttpContext\>\&) | 未实现。 |
## 另见

* Class [Object](../../system/object/)
* Namespace [System::Web](../)
* Library [Aspose.Page for C++](../../)
