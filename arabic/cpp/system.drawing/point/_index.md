---
title: "فئة System::Drawing::Point"
linktitle: "Point"
second_title: "Aspose.Page لـ C++"
description: "فئة System::Drawing::Point. تمثل زوجًا من إحداثيات X و Y الصحيحة لنقطة على سطح ثنائي الأبعاد. يجب تخصيص هذا النوع على المكدس وتمريره إلى الدوال بالقيمة أو بالمرجع. لا تستخدم أبدًا فئة System::SmartPtr لإدارة كائنات هذا النوع في C++."
type: docs
weight: 1700
url: /ar/cpp/system.drawing/point/
---
## Point class


تمثل زوجًا من إحداثيات X و Y الصحيحة لنقطة على سطح ثنائي الأبعاد. يجب تخصيص هذا النوع على المكدس وتمريره إلى الدوال بالقيمة أو بالمرجع. لا تستخدم أبدًا فئة [System::SmartPtr](../../system/smartptr/) لإدارة كائنات هذا النوع.

```cpp
class Point
```

## الطرق

| طريقة | الوصف |
| --- | --- |
| static [Add](./add/)(const Point\&, const Size\&) | يضيف قيم العرض والارتفاع لكائن [Size](../size/) المحدد إلى قيم إحداثيات X و Y لكائن [Point](./) المحدد على التوالي. |
| static [Ceiling](./ceiling/)(const PointF\&) | ينشئ كائن [Point](./) من كائن [PointF](../pointf/) المحدد عن طريق تقريب قيم إحداثيات X و Y لكائن [PointF](../pointf/) إلى أعلى قيمة صحيحة. |
| [Equals](./equals/)(const Point\&) const | يحدد ما إذا كان الكائن الحالي والكائن المحدد متساويين، أي يمثلان نفس زوج قيم إحداثيات X و Y. |
| [get_IsEmpty](./get_isempty/)() const | يحدد ما إذا كانت قيم إحداثيات X و Y كلاهما مساوية للصفر. |
| [get_X](./get_x/)() const | يعيد قيمة إحداثي X التي يمثلها الكائن الحالي. |
| [get_Y](./get_y/)() const | يعيد قيمة إحداثي Y التي يمثلها الكائن الحالي. |
| [GetHashCode](./gethashcode/)() const | يعيد رمز تجزئة للكائن الحالي. |
| [getStdHash](./getstdhash/)() const | يعيد قيمة تجزئة للكائن الحالي. |
| [IsNull](./isnull/)() const | دائمًا ما تُعيد false. |
| [Offset](./offset/)(int, int) | يضيف إزاحة إلى قيم إحداثيات X و Y الممثلة بواسطة الكائن الحالي بالقيم المحددة. |
| [Offset](./offset/)(Point) | يضيف إزاحة إلى إحداثيات X و Y الممثلة بواسطة الكائن الحالي بقيم إحداثيات X و Y الممثلة بواسطة كائن [Point](./) المحدد على التوالي. |
| [operator PointF](./operatorpointf/)() const | ينشئ مثيلًا لكائن [PointF](../pointf/) ويُهيئه بقيم إحداثيات X و Y لكائن [Point](./) الحالي. |
| [operator Size](./operatorsize/)() const | ينشئ مثيلًا لكائن [Size](../size/) ويُهيئ قيم العرض والارتفاع به بقيم إحداثيات X و Y الممثلة بواسطة الكائن الحالي على التوالي. |
| [Point](./point/)() | ينشئ كائنًا جديدًا من نوع [Point](./) ويُهيئ قيم إحداثيات X و Y به إلى 0. |
| [Point](./point/)(int, int) | ينشئ كائنًا جديدًا من نوع [Point](./) ويُهيئه بالقيم المحددة. |
| [Point](./point/)(const Size\&) | ينشئ كائنًا جديدًا من نوع [Point](./) ويُهيئ قيم إحداثيات X و Y به بقيم العرض والارتفاع لكائن [SizeF](../sizef/) المحدد على التوالي. |
| [Point](./point/)(int) | ينشئ كائنًا جديدًا من نوع [Point](./) ويُهيئ قيمة إحداثي X به بقيمة مكوّنة من الـ 16 بت العليا للعدد الصحيح 32‑بت المحدد، وقيمة إحداثي Y به بقيمة مكوّنة من الـ 16 بت السفلى للعدد الصحيح 32‑بت المحدد. |
| static [Round](./round/)(const PointF\&) | ينشئ كائن [Point](./) من كائن [PointF](../pointf/) المحدد عن طريق تقريب قيم إحداثيات X و Y لكائن [PointF](../pointf/) إلى أقرب قيم صحيحة. |
| [set_X](./set_x/)(int) | يضبط قيمة إحداثية X التي يمثلها الكائن الحالي. |
| [set_Y](./set_y/)(int) | يضبط قيمة إحداثية Y التي يمثلها الكائن الحالي. |
| static [Subtract](./subtract/)(const Point\&, const Size\&) | يطرح قيم العرض والارتفاع لكائن [Size](../size/) المحدد من قيم إحداثيات X و Y لكائن [Point](./) المحدد على التوالي. |
| [ToString](./tostring/)() const | يرجع تمثيل السلسلة للزوج من قيم إحداثيات X و Y التي يمثلها الكائن الحالي. |
| static [Truncate](./truncate/)(const PointF\&) | ينشئ كائن [Point](./) من كائن [PointF](../pointf/) المحدد عن طريق قطع قيم إحداثيات X و Y لكائن [PointF](../pointf/) إلى القيم الصحيحة الأدنى. |
## الحقول

| حقل | الوصف |
| --- | --- |
| static [Empty](./empty/) | مثيل فارغ من فئة [Point](./) تكون قيم إحداثيات X و Y فيه صفر. |
## انظر أيضًا

* Namespace [System::Drawing](../)
* Library [Aspose.Page for C++](../../)
