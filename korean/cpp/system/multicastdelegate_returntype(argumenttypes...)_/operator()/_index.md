---
title: "System::MulticastDelegate< ReturnType(ArgumentTypes...)>::operator() 메서드"
linktitle: "operator()"
second_title: "C++용 Aspose.Page"
description: "System::MulticastDelegate< ReturnType(ArgumentTypes...)>::operator() 메서드. 현재 대리자 컬렉션에 존재하는 모든 대리자를 호출합니다. 대리자는 컬렉션에 추가된 순서대로 호출됩니다. 이 연산자는 C++에서 대리자가 실행되는 동안 차단됩니다."
type: docs
weight: 1400
url: /ko/cpp/system/multicastdelegate_returntype(argumenttypes...)_/operator()/
---
## MulticastDelegate< ReturnType(ArgumentTypes...)>::operator() method


현재 대리자 컬렉션에 존재하는 모든 대리자를 호출합니다. 대리자는 컬렉션에 추가된 순서대로 호출됩니다. 연산자는 대리자가 실행되는 동안 차단됩니다.

```cpp
ReturnType System::MulticastDelegate<ReturnType(ArgumentTypes...)>::operator()(ArgumentTypes... args) const
```


| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| args | ArgumentTypes... | 호출될 대리자에게 전달할 인수 |

### ReturnValue

마지막으로 호출된 대리자의 반환값

## 또 보기

* Class [MulticastDelegate< ReturnType(ArgumentTypes...)>](../)
* Namespace [System](../../)
* Library [Aspose.Page for C++](../../../)
