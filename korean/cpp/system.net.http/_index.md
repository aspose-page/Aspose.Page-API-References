---
title: "System::Net::Http 네임스페이스"
linktitle: "System::Net::Http"
second_title: "C++용 Aspose.Page"
description: "C++에서 System::Net::Http 네임스페이스를 사용하는 방법."
type: docs
weight: 4400
url: /ko/cpp/system.net.http/
---



## 클래스

| 클래스 | 설명 |
| --- | --- |
| [ByteArrayContent](./bytearraycontent/) | HTTP 콘텐츠를 바이트 배열로 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpClient](./httpclient/) | 요청을 보내고 응답을 받기 위한 HTTP 클라이언트의 기본 클래스를 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpClientHandler](./httpclienthandler/) | [HttpClient](./httpclient/) 클래스에서 사용되는 기본 메시지 핸들러를 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpContent](./httpcontent/) | HTTP 엔터티의 콘텐츠를 나타냅니다. 이 클래스의 [Object](../system/object/)는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpMessageHandler](./httpmessagehandler/) | HTTP 메시지 핸들러의 기본 유형을 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpMessageInvoker](./httpmessageinvoker/) | 애플리케이션이 HTTP 핸들러 체인에서 Send 메서드를 호출할 수 있도록 합니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpMethod](./httpmethod/) | HTTP 메서드를 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpRequestMessage](./httprequestmessage/) | HTTP 요청 메시지를 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpResponseMessage](./httpresponsemessage/) | HTTP 응답 메시지를 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
| [HttpUtilities](./httputilities/) | 유틸리티 메서드를 포함합니다. |
| [StringContent](./stringcontent/) | HTTP 콘텐츠를 문자열로 나타냅니다. 이 클래스의 객체는 반드시 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 인스턴스를 생성하거나 operator new를 사용하면 런타임 오류 및/또는 어설션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 함수 인자로 전달하십시오. |
## Enums

| 열거형 | 설명 |
| --- | --- |
| [HttpCompletionOption](./httpcompletionoption/) | 어떤 시점에 [HttpClient](./httpclient/) 작업이 완료되어야 하는지를 나타냅니다. |
| [HttpParseResult](./httpparseresult/) | 구문 분석 결과를 나타냅니다. |
## Typedefs

| 타입 정의 | 설명 |
| --- | --- |
| [HttpRequestException](./httprequestexception/) |  |
## Functions

| 함수 | 설명 |
| --- | --- |
| operator!= | System.Collections.Generic.List`1[Doxygen2HugoConverter.Markup.SimpleMarkupEntry] |
| operator== | System.Collections.Generic.List`1[Doxygen2HugoConverter.Markup.SimpleMarkupEntry] |
